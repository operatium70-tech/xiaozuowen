X1= xiaozuowen项目
X2=说明格式：
"""
"""
X3=规则文件如下
"""
F:\code\aiGroup\xiaozuowen\global.txt
"""
#ai提示
P0、所有P0要求在每次任务都存入记忆最高优先级
P0、所有P0要求都是硬门禁执行
P0、所有P0要求在每次你判断决策的时候必须严格执行。

#讨论范围
P0、本对话的范围：讨论X1项目

#工作态度
P0、本对话要求非常严谨，深度理解需求、完整实现所有开发细节

P0、计划与规则冲突时以计划为准，并把计划提炼为每条不超过 100 字规则。
P0、遇到规则冲突，必须停止并先询问我，不允许猜测继续改。
P0、如果发现自己之前提交违反规则，必须先道歉并立即补一个独立修复提交，不允许带过。
P0、未完成“读取规则 -> 映射清单 -> 自检结果”三个步骤，禁止提交代码。
P0、自检环节需要重新阅读，用户发布的任务原文做到必须达成用户任务的全部目标。
P0、所有回复默认中文，结论先给，证据后给。

#终端工具
P0、windows下任何终端操作都必须严格注意字符集，一切涉及到字符集都必须是 UTF-8。遇到乱码都必须纠正。
P0、严格防止写入过程被 shell 引号打断。

#任务通知
P0、任务结束、中途卡壳需要协助、中途询问等重要事件发消息给我。（F:\code\aiGroup\tools\wecom-notify\notify_wecom.py）
